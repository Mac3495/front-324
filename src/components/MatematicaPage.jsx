import React from 'react';
import 'firebase/auth';
import { useFirebaseApp } from 'reactfire';

export default (props) =>{
    return(
        <div>
            <div>
                <p>Matematica</p>
            </div>
        </div>
    )
}